

import { ComponentFixture, TestBed } from '@angular/core/testing';

/*import { Tipos } from './formularios.expedientes.component';

describe('FormulariosExpedientesComponent', () => {
  let component: Formularios;
  let fixture: ComponentFixture<Formularios>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [Formularios]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(Formularios);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/